LogViewer {
    height: 1fr;
    width: 100%;
    overflow-y: auto;
    padding: 1 2;
    background: $surface;
    border: wide $primary;
}

.log-entry {
    display: block;
    padding: 0 1;
    color: $text;
}
