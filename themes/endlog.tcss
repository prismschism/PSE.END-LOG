#log-viewer {
    height: 1fr;
    width: 100%;
    overflow-y: auto;
    border: solid $primary;
    scrollbar-color: $secondary;
    background: $boost;
}

.log-entry {
    display: block;
    color: $text;
    padding: 0 1;
}
